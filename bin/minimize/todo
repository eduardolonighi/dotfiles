necesito hacer un script que devuelva los titulos y los ides del json pa que polybar los lea(no se si polibar lee json) y hacer el populate de la lista con los clicks que va a correr el maximize con el id